define(function (require) {
    var $ = require('jquery'),
        lib = require('./helpers');

    //A fabricated API to show interaction of
    //common and specific pieces.
    $(function () {
        console.log('A fabricated API to show interaction of common and specific pieces.');
    });
});
